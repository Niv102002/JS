#include "demo.h"
template<class T> stack<T>::stack()
{
	pointer = 0;
	size = 0;
	top = 0;
}

template<class T> stack<T>::stack (int s)
{
	//pointer = p;
	size = s;
	//top = t;
}

template<class T>
T stack<T>::pop()
{
	for ()
}

template<class T>
void stack<T>::push()
{
	int arr[]
	arr = new int[size]
	cout<<"\nEnter Values : "<<value;
	for (i=0;i<size,i++)
	{
		cin<<
	}
}

template<class T>
void stack<T>::display()
{
	cout<<"\nValue : "<<value;
}
